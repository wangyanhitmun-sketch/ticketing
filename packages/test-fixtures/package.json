{
  "name": "@ticketing/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "工单系统测试数据工厂与种子数据辅助工具。",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node -e \"console.log('fixtures tests pending')\"",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./dist/index.d.ts"
}
